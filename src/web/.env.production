# Version: v1
# Required: HTTPS endpoint for production API
VITE_API_URL=https://api.detection-platform.com/api/v1

# Auth0 Authentication Configuration
# Version: Latest
# Required: Production Auth0 tenant settings
VITE_AUTH0_DOMAIN=detection-platform.auth0.com
VITE_AUTH0_CLIENT_ID=production_client_id
VITE_AUTH0_AUDIENCE=https://api.detection-platform.com

# WebSocket Configuration
# Version: v1
# Required: Secure WebSocket endpoint for real-time features
VITE_WEBSOCKET_URL=wss://ws.detection-platform.com

# Environment Configuration
# Required: Production environment identifier
VITE_ENVIRONMENT=production

# Logging Configuration
# Required: Restricted to error-level for production
VITE_LOG_LEVEL=error

# Security Notice:
# - All endpoints must use HTTPS/WSS protocols
# - Auth0 client ID should be managed via secure vault in deployment
# - SSL certificates must be valid and up-to-date
# - MFA should be enforced through Auth0 configuration
# - Error logging only to prevent sensitive data exposure